<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.doom.s1.qnacheck.QnaCheckDAO">
  	<insert id="qnaCheckInsert" >
  		insert into qnacheck values(qckey_seq.nextval,#{id}, #{qc_check},#{qna_storekey})
  	</insert>
  	
  	<insert id="qnaOK" parameterType="Long">
  		insert into storelist values(
  		(select qna_phone from qna where qna_storekey=#{qna_storekey}),
  		(select qna_title from qna where qna_storekey=#{qna_storekey}),
  		#{qna_storekey},
  		(select qna_contents from qna where qna_storekey=#{qna_storekey}),
  		(select id from qna where qna_storekey=#{qna_storekey})
  		)
  	</insert>
  
  	<delete id="qnaOkDel" parameterType="Long">
  		delete qna where qna_storekey=#{qna_storekey}
  	</delete>
  	
  	<insert id="storeFileInsert" parameterType="com.doom.qna.qnaFile.QnaFileVO">
  		insert into storefile values(#{qna_storekey},#{qf_key},#{qf_filename},#{qf,oriname})
  	</insert>
  	
  	<insert id="storeMenuInsert" parameterType="Long">
  		insert into storemenu values((select qm_menu from qnamenu where qna_storekey = #{qna_storekey}),
  		(select qm_price from qnamenu where qna_storekey = #{qna_storekey}), 
  		smkey_seq.nextval, #{qna_storekey})
  	</insert>

	<select id="Statuscheck" parameterType="String" resultType="com.doom.s1.qna.QnaVO">
		select * from qna where id=#{id}
	</select>

	<select id="selctFile" parameterType="Long" resultType="com.doom.s1.qna.qnaFile.QnaFileVO">
		select * from qnafile where qna_storekey=#{qna_storekey}
	</select>

	
  	
  </mapper>