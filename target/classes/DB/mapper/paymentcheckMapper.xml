<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.doom.s1.paymentcheck.PaymentCheckDAO">
  <!--결제 하기전에  영수증같이 출력할꺼야  -->
  
   <insert id="paymentCheckReady" parameterType="Long">
		insert into paymentcheck values(
		#{id},<!--아이디  -->
		pay_seq.nextval,<!-- 시퀀스 -->
  		#{st_key},<!-- 101번 -->
  		#{sm_key},<!-- 46번 -->
  		(select sm_menu from storemenu where st_key=#{st_key}), <!-- 메뉴 -->
  		(select qna_contents from qna where qna_storekey=#{qna_storekey}), <!-- 수량 -->
  		(select qna_contents from qna where qna_storekey=#{qna_storekey}) <!-- 가격 -->
	
		)
	</insert>
	
	<insert id="paymentInsert" parameterType="Long">
		insert into payment values(
		#{pc_num},
		#{pay_num},
		#{pay_total}
		)
	
	</insert>
  
  
  
  
  
  </mapper>