<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.doom.s1.storeList.storeMenu.StoreMenuDAO">

	<select id="storeMenuSelect" parameterType="java.lang.Long" resultType="com.doom.s1.storeList.storeMenu.StoreMenuVO">
		SELECT * FROM storeMenu WHERE st_key=#{st_key} ORDER BY sm_key ASC
	</select>
	
</mapper>